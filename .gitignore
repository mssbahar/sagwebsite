# ── Build output (Vercel/Netlify rebuild from source) ──
dist/
.astro/

# ── Dependencies (installed via npm ci on deploy) ──
node_modules/

# ── Secrets ──
.env
.env.*
!.env.example

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Deploy tool caches (local only) ──
.vercel/
.netlify/

# ── AI / dev tooling (not part of the website) ──
.agents/
.cursor/
.firecrawl/
skills-lock.json
AGENTS.md
CLAUDE.md
assets.md
design.md

# ── Local source assets (keep originals off GitHub; site uses /public) ──
ASSETS/
